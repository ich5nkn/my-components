name: CI Actions

on: push
jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - name: example
        run: |
          echo "example"
          ls -a
          npm -v
      # - name: install
      #   run: npm i
      # - name: build
      #   run: npm run build
